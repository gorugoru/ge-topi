= form_for @getopi do |f|
  - if @getopi.errors.any?
    #error_explanation
      %h2= "#{pluralize(@getopi.errors.count, "error")} prohibited this getopi from being saved:"
      %ul
        - @getopi.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
